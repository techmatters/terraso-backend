[project]
name = "terraso_backend"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.12"

[tool.ruff]
exclude = ["migrations"]
line-length = 100

[tool.ruff.lint.isort]
known-first-party = ["apps"]

[tool.ruff.lint]
extend-ignore = ["E203"]
